# CHANGELOG

## v2.2 (2026-09-26) — 路径处理加固
统一并加固所有路径类接口的 `path` 解析，修复含中文/空格/混合分隔符时定位文件不准确的问题。

### 新增 / New
- 统一的路径规范化工具：`ConvertTo-AgentPath` 与 `Get-QueryParam`。
  - 自动把 `/` 转成 `\`，折叠连续反斜杠（`C:\\dir` → `C:\dir`，UNC 双反斜杠除外）。
  - 自动去除传入路径外层包裹的双引号/单引号。
  - 正确处理盘符根目录（`C:` → `C:\`）与 UNC 路径（`\\host` 保留双斜杠）。
  - `?path=` 参数自动 URL 解码；省略 `path` 时默认当前目录 `.`。
- 以下接口的 `path` 全部统一走该解析：`/ls`、`/tail`、`/file`(GET/POST)、`/zip`、`/unzip`。

### 修复 / Fixed
- 路径含中文 / 空格 / 混合 `/\` 分隔符时可能找不到文件、或拼接出错误路径的问题。
- `?path=` 带 URL 编码（`%20`、`%uXXXX`）时未解码直接使用的问题。

### 兼容性 / Compatibility
- 接口与认证方式无任何变更，v2.1 客户端可直接使用。版本号升至 2.2。

---

## v2.1 (2026-09-24) — 并发 Worker + 防爆破 + 中文化启动器
- 每请求独立 runspace 执行，长命令不再卡死 `/health`。
- fail2ban：同 IP 300s 内 5 次密码错误锁定 900s，返回 HTTP 429；`/fail` GET/POST 管理封禁表。
- 启动器自动生成一整段可复制的 AI 引导提示词，并放在控制台最底部。

---

## v2.0 — 并发修复
- 单线程监听改为每请求独立 worker，解决命令挂起导致整个 agent 无响应的问题。